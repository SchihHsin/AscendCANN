# CANN 角色与旅程体验报告（基于 context.md 骨架 + 新分析补全）

更新时间：2026-05-14

这版报告把 `context.md` 作为角色/场景/旅程骨架；如果 context 里缺内容，但新分析已有可用内容，则使用新分析补全，并明确标注来源。

## 算子开发者

角色均分：`85.81`

### 算子复现部署

场景分：`81.8`
- 感知学习：`90.83`，顺滑。该阶段是否顺滑，决定用户能否自然进入下一步。
- 环境准备：`80.3`，可完成，但仍有跳转成本。文档能指路，但从说明到可运行环境之间仍有明显操作落差。
- 环境验证：`74.27`，可完成，但仍有跳转成本。关键不是能不能跑，而是跑完后能否快速确认环境真的可用。

### 算子迁移部署

场景分：`90.7`
- 感知学习：`97.21`，顺滑。该阶段是否顺滑，决定用户能否自然进入下一步。
- 算子迁移：`96.0`，顺滑。该阶段是否顺滑，决定用户能否自然进入下一步。
- 算子编译：`90.0`，顺滑。该阶段是否顺滑，决定用户能否自然进入下一步。
- 功能调测：`83.26`，可完成，但仍有跳转成本。用户最怕的是失败后不知道该回到哪一步。
- 性能调优：`87.01`，顺滑。这一段更像反复试探，缺少稳定的优化闭环提示。

### Builtin 算子定制修改

场景分：`86.5`
- 感知学习：`96.07`，顺滑。该阶段是否顺滑，决定用户能否自然进入下一步。
- 算子设计与实现：`89.67`，顺滑。该阶段是否顺滑，决定用户能否自然进入下一步。
- 算子编译：`87.5`，顺滑。该阶段是否顺滑，决定用户能否自然进入下一步。
- 功能调测：`75.89`，可完成，但仍有跳转成本。用户最怕的是失败后不知道该回到哪一步。
- 性能调优：`83.39`，可完成，但仍有跳转成本。这一段更像反复试探，缺少稳定的优化闭环提示。

### 算子基本功能实现

场景分：`86.41`
- 感知学习：`91.13`，顺滑。该阶段是否顺滑，决定用户能否自然进入下一步。
- 算子设计与实现：`91.88`，顺滑。该阶段是否顺滑，决定用户能否自然进入下一步。
- 算子编译：`87.33`，顺滑。该阶段是否顺滑，决定用户能否自然进入下一步。
- 功能调测：`77.42`，可完成，但仍有跳转成本。用户最怕的是失败后不知道该回到哪一步。
- 性能调优：`84.29`，可完成，但仍有跳转成本。这一段更像反复试探，缺少稳定的优化闭环提示。

### 特定 shape 性能优化

该旅程在 context.md 中未展开，这里基于新分析对算子/库开发者的调优阶段补全。

场景分：`85.79`
- 感知学习：`96.62`，顺滑。该阶段是否顺滑，决定用户能否自然进入下一步。
- 功能调测：`77.75`，可完成，但仍有跳转成本。用户最怕的是失败后不知道该回到哪一步。
- 性能调优：`83.0`，可完成，但仍有跳转成本。这一段更像反复试探，缺少稳定的优化闭环提示。

### 泛化 shape 性能优化

该旅程在 context.md 中未展开，这里用新分析结果补齐泛化 shape 优化内容。

场景分：`83.67`
- 感知学习：`93.35`，顺滑。该阶段是否顺滑，决定用户能否自然进入下一步。
- 功能调测：`75.5`，可完成，但仍有跳转成本。用户最怕的是失败后不知道该回到哪一步。
- 性能调优：`82.15`，可完成，但仍有跳转成本。这一段更像反复试探，缺少稳定的优化闭环提示。

## AI 框架开发者

角色均分：`72.14`

### 裸模型 Torch 原生开发

context.md 只有场景名，没有旅程内容；这里用新分析中的“模型/框架适配工程师”结果补出原生开发的基础旅程。

场景分：`75.64`
- 感知学习：`90.62`，顺滑。该阶段是否顺滑，决定用户能否自然进入下一步。
- 环境准备：`86.38`，顺滑。前置依赖分散在不同组件里，用户需要的是单条准备路径。
- 环境验证：`49.92`，高阻塞。该阶段是否顺滑，决定用户能否自然进入下一步。

### 裸模型昇腾亲和优化

场景分：`76.03`
- 感知学习：`89.67`，顺滑。该阶段是否顺滑，决定用户能否自然进入下一步。
- 整网优化：`62.39`，需要额外摸索。跨仓库拼接感强，用户需要统一路径，而不是散点材料。

### VLLM/SGL 框架原生开发

该内容由新分析补全，作为框架原生开发的当前可观察代理场景。

场景分：`65.18`
- 感知学习：`89.58`，顺滑。该阶段是否顺滑，决定用户能否自然进入下一步。
- 环境准备：`63.0`，需要额外摸索。前置依赖分散在不同组件里，用户需要的是单条准备路径。
- 环境验证：`42.97`，高阻塞。该阶段是否顺滑，决定用户能否自然进入下一步。

### VLLM/SGL 框架昇腾亲和优化

该内容由新分析补全，突出跨后端适配与优化的连续性问题。

场景分：`73.14`
- 感知学习：`89.25`，顺滑。该阶段是否顺滑，决定用户能否自然进入下一步。
- 整网优化：`57.04`，需要额外摸索。跨仓库拼接感强，用户需要统一路径，而不是散点材料。

### Verl 框架原生开发

该内容由新分析补全，作为训练框架原生开发的可观察代理。

场景分：`67.11`
- 感知学习：`90.0`，顺滑。该阶段是否顺滑，决定用户能否自然进入下一步。
- 环境准备：`66.25`，需要额外摸索。前置依赖分散在不同组件里，用户需要的是单条准备路径。
- 环境验证：`45.09`，高阻塞。该阶段是否顺滑，决定用户能否自然进入下一步。

### Verl 框架昇腾亲和优化

该内容由新分析补全，用来补足训练框架优化链路。

场景分：`75.74`
- 感知学习：`89.42`，顺滑。该阶段是否顺滑，决定用户能否自然进入下一步。
- 整网优化：`62.06`，需要额外摸索。跨仓库拼接感强，用户需要统一路径，而不是散点材料。

## 应用开发者

角色均分：`51.29`

### 行业方案验证与 PoC

该场景不在 context.md 原文中展开，基于新分析中的“行业方案开发者”旅程进行映射补全。

场景分：`41.87`
- 感知学习：`64.04`，需要额外摸索。该阶段是否顺滑，决定用户能否自然进入下一步。
- 环境准备：`43.38`，高阻塞。应用型用户希望尽快拉起最小场景，不希望先理解复杂依赖树。
- 环境验证：`18.19`，高阻塞。对业务用户而言，验证的是“能否支撑 PoC”，而不只是命令执行成功。

### 样例落地与快速试用

该场景基于新分析中的“样例体验型开发者”补到应用开发者内容层，用来补齐应用侧的上手旅程。

场景分：`60.71`
- 感知学习：`83.75`，可完成，但仍有跳转成本。该阶段是否顺滑，决定用户能否自然进入下一步。
- 环境准备：`53.14`，高阻塞。应用型用户希望尽快拉起最小场景，不希望先理解复杂依赖树。
- 环境验证：`45.25`，高阻塞。对业务用户而言，验证的是“能否支撑 PoC”，而不只是命令执行成功。

## 入门开发者

角色均分：`72.53`

### S0 搜索与发现

场景分：`86.6`
- S0 搜索与发现：`86.6`，顺滑。入口是否直达，决定新用户会不会继续往下走。

### S1 环境检查与准备

场景分：`67.0`
- S1 环境检查与准备：`67.0`，需要额外摸索。核心是预检、依赖和成功判定是否足够明确。

### S2 样例快速体验

context.md 只有阶段名；这里沿用新分析中“样例体验型开发者”的结果，把内容层补进社区新人旅程。

场景分：`57.25`
- S2 样例快速体验：`57.25`，需要额外摸索。新人希望最短路径看到结果，而不是先理解全貌。

### S3 开发与编译

context.md 只有阶段名；这里用新分析中的开发/编译观测来补全内容层。

场景分：`89.17`
- S3 开发与编译：`89.17`，顺滑。该阶段是否顺滑，决定用户能否自然进入下一步。

### S4 测试与验证

context.md 只有阶段名；这里用新分析中的验证观测来补全内容层。

场景分：`84.17`
- S4 测试与验证：`84.17`，可完成，但仍有跳转成本。该阶段是否顺滑，决定用户能否自然进入下一步。

### S5 反馈与贡献

context.md 只有阶段名；这里用新分析中的社区反馈入口观察来补全内容层。

场景分：`51.0`
- S5 反馈与贡献：`51.0`，高阻塞。如果贡献入口不清晰，社区旅程会在最后一步断掉。

