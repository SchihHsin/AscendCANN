# 本地验证记录 · 2026-09-14

## 交付范围

- `index.html`：18 页 Ploy 交互研究与昇腾迁移提案；Demo 在第 11–16 页。
- `demo.html`：独立前端原型，6 个起始场景；支持手动操作及汇报内脚本回放。
- 官网、文档与官方视频片段为研究来源。未登录 Ploy 账号实测；未观看长视频全程。详见 `evidence/evidence-notes.md`。
- AI、视频课件、代码运行、NPU 结果均为模拟；当前只支持 ONNX 场景与预设修改指令。未接模型 API、真实视频、终端或 NPU。

## 已执行检查

- demo.js、model.mjs 语法检查通过；index.html 两段内联脚本解析通过。
- test-model.mjs 通过：范围限定、完成/锁定保护、前置依赖、恢复进度和未知指令。
- index.html 的本地 src / href / data-image 均可解析到已有文件。
- 本地应用内浏览器 1280 × 720：18 页均为 720px 页面，标题、正文区域、页脚与 Demo 外框未越出页面；抽查证据页 #4、#5 和 Demo #11–16 的实际截图。
- #4 采用 CSS 局部放大标注区域，点击打开未经改写的原始视频截图；#5 前后文字修改对照可辨认。图片弹窗打开和关闭验证通过。
- 已手动走通：首页输入云端目标 → 生成路径；云端修改预览 → 确认 → 节点由 35 分钟变为 20 分钟 → 撤销。
- 已手动走通：失败诊断 → 插入修复节点 → 跟练核对 → 返回运行 → 模拟成功。
- 已手动走通：复用路线 → 新模型任务 → 查看原任务记录。新任务重置进度，原记录分开保留。
- 验证 #13 交互演示播放与 hash 跳页；页面刷新可恢复当前页。浏览器日志抽查无 error / warn。
- 缩放汇报内 Demo，使首页入口、Diff 确认、复用表单完整可见；长路径与内容区域可独立滚动。
- git diff --check 通过。仅新增本目录并追加 AGENTS.md；旧汇报与 V2 未在本轮修改。

## 尚待验证

- 未完成移动端和全部键盘/读屏验证；未对所有组合操作逐一测试。
- 运行规则用于说明交互，不验证任意用户代码、模型兼容性或真实学习成效。
- Ploy 的效率收益为 UX 推论，需要实际用户任务测试。
- 本地未 commit、push、部署；保留用户原有未提交文件。

## 发布更新 · 2026-09-14

以上“仅本地”是首次交付时的记录。用户随后要求每次改完推送，研究、Demo、证据与新补充报告已随 `3557a08e` 推送 origin/main；与发布无关的文件保持原状。
